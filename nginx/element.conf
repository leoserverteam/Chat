server {
    listen 80;
    listen [::]:80;
    server_name element.leonet.site;

    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;
    server_name element.leonet.site;

    ssl_certificate /etc/letsencrypt/live/element.leonet.site/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/element.leonet.site/privkey.pem;

    location / {
        proxy_pass http://localhost:8081;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
